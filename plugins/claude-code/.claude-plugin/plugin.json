{
  "name": "mnemoseed",
  "version": "0.1.0",
  "description": "Persistent, provenance-first memory for Claude Code — captures every turn, warms up sessions with the latest context, and answers recall, dream, forget, and memory-status slash commands",
  "author": {
    "name": "MnemoSeed"
  },
  "license": "MIT",
  "commands": "./commands",
  "hooks": "./hooks/hooks.json",
  "mcpServers": {
    "mnemoseed": {
      "command": "mnemoseed",
      "args": ["mcp"],
      "env": {
        "MNEMOSEED_PROFILE_ID": "${MNEMOSEED_PROFILE_ID}",
        "MNEMOSEED_TOKEN": "${MNEMOSEED_TOKEN}"
      }
    }
  }
}
